# robustrolling (Python)

Python bindings for fast rolling window metrics implemented in C++ and exposed via pybind11.

## Install

```bash
pip install .
```

For development:

```bash
pip install -e .[dev]
pytest
```
